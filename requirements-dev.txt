# Test-only dependencies. Not installed by Vercel (only requirements.txt is used
# for the serverless build) -- install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest
httpx
